# Web / dashboard
Flask>=3.0
Jinja2>=3.1

# Config
python-dotenv>=1.0
PyYAML>=6.0

# Page fetching
# 버전을 Dockerfile 의 Playwright 베이스 이미지 태그(v1.58.0)와 일치시킨다.
playwright==1.58.0

# Extraction
anthropic>=0.39

# Schema validation
pydantic>=2.7

# Scheduling (internal mode)
APScheduler>=3.10

# Production WSGI servers
gunicorn>=22.0 ; sys_platform != "win32"
waitress>=3.0
